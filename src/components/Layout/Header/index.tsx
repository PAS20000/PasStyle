import * as React from 'react'
import PasStyle from '../../..'
import { PasStyleProps } from '../../../../utils/types'

const Header = (props : PasStyleProps) => {

    return <PasStyle.Header {...props}/>
}

export default Header
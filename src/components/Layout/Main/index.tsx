import * as React from 'react'
import PasStyle from '../../..'
import { PasStyleProps } from '../../../../utils/types'

const Main = (props : PasStyleProps) => {

    return <PasStyle.Main {...props}/>
}

export default Main